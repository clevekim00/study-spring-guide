spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: test
    password: test
# If you get a Table 'BATCH_JOB_INSTANCE' doesn't exist error at the first execution, you need to add this setting.
  batch:
    jdbc:
      initialize-schema: always
